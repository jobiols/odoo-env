# Archive Report: install-by-client-name

**Status**: ✅ PASS
**Date**: 2026-06-15
**Executor**: sdd-archive (parent-orchestrated)

---

## Executive Summary

Archived the `install-by-client-name` change. `oe -i <client>` now builds the canonical
repo URL `git@github.com:<org>/cl-<client>.git` from a bare client name, while full git
URLs keep working. Organization is configurable via `--org` (persistent) with a
`quilsoft-org` default that is persisted when missing. All phases complete: proposal,
spec, tasks, apply, verify, sync, archive. 208/208 tests pass, zero regressions.
pre-commit (all hooks) passes on all files.

---

## Artifacts

| Artifact | Path | Status |
|----------|------|--------|
| Proposal | `proposal.md` | ✅ |
| Spec (client) | `specs/client/spec.md` | ✅ 10 requirements (1 MODIFIED, 3 ADDED) |
| Tasks | `tasks.md` | ✅ all complete |
| Verify Report | `verify-report.md` | ✅ PASS — 208 tests |
| Sync Report | `sync-report.md` | ✅ canonical spec updated |

---

## Sync Summary

| Domain | Operation | Requirements |
|--------|-----------|-------------|
| `client` | Full copy → `openspec/specs/client/spec.md` | 1 MODIFIED (003), 3 ADDED (008/009/010), 6 unchanged |

---

## Verification

- Tests: `PYTHONPATH=. venv/bin/python -m unittest discover -s odoo_env -p "test_*.py"` → 208 OK.
- Lint: `pre-commit run --all-files` → all hooks pass.
- LSP/Pyright: 0 errors on changed source/test files.

## Implementation Files

- `odoo_env/config.py` — `get_organization()`, `save_organization()`, persist `--org`.
- `odoo_env/oe.py` — `-i` metavar `CLIENT` + help, `--org` flag, standalone guard.
- `odoo_env/client.py` — `_is_full_git_url()`, `build_repo_url()`, `_resolve_install_url()`, wiring.
- `odoo_env/test_helpers.py` — `org` default + `OeConfigPatchTestCase` base.
- `odoo_env/test_config.py` (new), `odoo_env/test_oe_args.py` (new), `odoo_env/test_client.py`, `odoo_env/test_oe.py`.
- `doc/uml/*.plantuml` — regenerated by pyreverse hook.
- `odoo_env/qa/runner.py` — incidental pre-existing lint fix (empty comment).

## Notes / Residual

- REQ-INSTALL-003 changed meaning: a non-URL string is now a valid client name (builds
  canonical URL); only empty / spaced / slashed names raise `OeError`.
- The `# nosec B105 B106` finding in `qa/runner.py` is a pre-existing false positive
  (Docker `--link` env vars), left untouched.
- Fresh adversarial diff review was skipped at user request.
